const db=require('./electron/database.cjs'); const order = { orderNumber: '123', tableId: 'T1', items: [], status: 'New', totalAmount: 50 }; const created = db.createOrder(order); console.log('Created:', created.id); const updated = db.completeOrderPayment(created.id, 'Cash'); console.log('Updated status:', updated.paymentStatus, 'paidAt:', updated.paidAt); console.log(new Date(updated.paidAt).toDateString() === new Date().toDateString());
